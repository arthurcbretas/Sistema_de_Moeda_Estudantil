# Render: a variável VITE_API_URL é injetada no build pela Vercel.
# Na Vercel Dashboard → sme-frontend → Settings → Environment Variables → adicione:
# VITE_API_URL = https://<url-do-backend-no-render>/api
# Placeholder — será atualizado com a URL real após o primeiro deploy do backend no Render.
VITE_API_URL=https://sme-backend.onrender.com/api
